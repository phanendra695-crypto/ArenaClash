name: arenaclash
description: ArenaClash Tournament App
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^2.25.4
  firebase_auth: ^4.17.8
  cloud_firestore: ^4.15.8
  razorpay_flutter: ^1.3.7

flutter:
  uses-material-design: true
